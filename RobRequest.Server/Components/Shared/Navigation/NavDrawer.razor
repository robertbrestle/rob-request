@inject CurrentUserService CurrentUser

@if (IsEnabled)
{
    <div class="toggle-drawer-button">
        <MudIconButton Icon="@Icons.Material.Filled.Menu"
                       Color="Color.Inherit"
                       Size="Size.Small"
                       Class="mt-2"
                       OnClick="OnDrawerToggle"
                       aria-label="toggle drawer"/>
    </div>

    <MudDrawer Open="IsDrawerOpen" OpenChanged="IsDrawerOpenChanged" Elevation="2" ClipMode="DrawerClipMode.Always" Width="300px"
               Class="drawer-container">
        <MudStack Row="true" Justify="Justify.Center" AlignItems="AlignItems.Center">
            <MudLink Href="/" Color="Color.Inherit">
                <MudStack Row="true" AlignItems="AlignItems.Center" Class="pl-8">
                    <MudImage Src="/favicon.png" Height="24"/>
                    <MudText Typo="Typo.h6" Class="mr-4">
                        RobRequest
                    </MudText>
                </MudStack>
            </MudLink>
            <MudSpacer/>
            <MudMenu Icon="@Icons.Material.Filled.MoreVert"
                     AnchorOrigin="Origin.BottomRight" TransformOrigin="Origin.TopRight">
                <MudMenuItem Icon="@Icons.Material.Filled.Folder"
                             Href="/collections">
                    Collections
                </MudMenuItem>
                <MudMenuItem Icon="@Icons.Material.Filled.Dns"
                             Href="/environments">
                    Environments
                </MudMenuItem>
                <MudDivider/>
                <MudMenuItem
                    Icon="@(IsDarkMode ? Icons.Material.Filled.LightMode : Icons.Material.Filled.DarkMode)"
                    OnClick="OnToggleDarkMode">
                    Theme
                </MudMenuItem>
                <MudMenuItem Icon="@Icons.Material.Filled.Settings"
                             Href="/settings">
                    Settings
                </MudMenuItem>
                <AuthorizeView Policy="Admin">
                    <Authorized>
                        <MudDivider/>
                        <MudMenuItem Icon="@Icons.Material.Filled.AdminPanelSettings"
                                     Href="/admin/users">
                            User Management
                        </MudMenuItem>
                    </Authorized>
                </AuthorizeView>
                <MudDivider/>
                <MudMenuItem Icon="@Icons.Material.Filled.Logout"
                             Href="/api/auth/logout"
                             ForceLoad="true">
                    Logout (@CurrentUser.Username)
                </MudMenuItem>
            </MudMenu>
        </MudStack>
        <SectionOutlet SectionName="Drawer"/>
    </MudDrawer>
}

@code {
    [Parameter] public bool IsEnabled { get; set; }
    [Parameter] public bool IsDrawerOpen { get; set; }
    [Parameter] public bool IsDarkMode { get; set; }
    [Parameter] public EventCallback<bool> IsDrawerOpenChanged { get; set; }
    [Parameter] public EventCallback OnToggleDarkMode { get; set; }
    [Parameter] public EventCallback OnDrawerToggle { get; set; }
}
